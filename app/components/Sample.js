import React from 'react'

function Sample({ image,
    title,
    _id,
    price,
    rating,
    originalPrice,
    badge,
    description,
    variants,
    stock,
    reviews }) {
  return (
    <div>Sample</div>
  )
}

export default Sample